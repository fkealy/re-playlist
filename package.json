{
  "name": "express-react-app",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.5.1"
  },
  "scripts": {
    "client": "yarn workspace @replaylist/client",
    "common": "yarn workspace @replaylist/common",
    "server": "yarn workspace @replaylist/server",
    "dev": "concurrently \"yarn server dev --verbose\" \"yarn client start --watch\"",
    "start": "yarn workspace @replaylist/client start"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^1.9.3",
    "concurrently": "^7.6.0",
    "lodash": "^4.17.21",
    "nodemon": "^2.0.22",
    "react-query": "^3.39.3",
    "react-scripts": "^5.0.1"
  }
}
